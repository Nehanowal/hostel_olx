{
  "name": "hostel-olx",
  "private": true,
  "scripts": {
    "setup": "npm --prefix server_side ci && npm --prefix client_side ci",
    "dev": "node scripts/dev.mjs",
    "build": "npm --prefix client_side run build",
    "start": "npm --prefix server_side start",
    "test": "npm --prefix server_side test && npm --prefix client_side test"
  },
  "engines": {
    "node": "24.x"
  },
  "devDependencies": {
    "prettier": "^3.9.6"
  }
}
