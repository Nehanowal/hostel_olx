# Vercel configuration only; this is not embedded in the browser bundle.
# Set in Vercel > Project Settings > Environment Variables before deployment.
# Keep /api requests relative; no VITE_API_URL is needed.
BACKEND_ORIGIN=https://YOUR-RENDER-SERVICE.onrender.com
# Same random secret as Render; never prefix this with VITE_.
API_PROXY_SECRET=
